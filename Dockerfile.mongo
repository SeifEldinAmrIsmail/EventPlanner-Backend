# Custom MongoDB image for EventPlanner (Database Dockerfile)
FROM mongo:6

# Optional: just documenting intended DB name
ENV MONGO_INITDB_DATABASE=eventplanner

# Port, volumes, etc. will be handled by docker-compose
